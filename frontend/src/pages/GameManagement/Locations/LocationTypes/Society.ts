export type Society = {
   history: string | null,
   religion: string | null,
   technology: string | null,
   culture: string | null,
   government: string | null,
   economy: string | null,
   secrets: string | null,
   allies: string | null,
   enemies: string | null,
}